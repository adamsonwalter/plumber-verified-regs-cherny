# OS metadata
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Backup / scratch files
*.bak
*.bak-*
*.swp
*.swo
*~

# ZCode / agent session state (tooling, not app)
.zcode/

# Claude Code local settings + local preview server config (tooling, not app)
.claude/

# Netlify CLI local cache
.netlify/

# Proposed-register sidecars written by the scheduled agent during local runs.
# (In production these are committed via the publish token, not left on disk.)
register.proposed.*.json

# Fetch artefacts from scripts/add_standard_pointers.py (re-fetched on each run)
scripts/_bpc/
